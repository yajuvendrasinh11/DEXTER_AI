# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.venv/

# Secrets
.env

# Data / Persistence
*.json
!analytics.json
!history.json
!users.json
# Wait, usually we don't commit these if they contain real user data.
# I'll exclude them but maybe keep dummy versions if needed.
# Let's exclude all json files that look like data.

users.json
history.json
analytics.json

# IDEs
.vscode/
.idea/
